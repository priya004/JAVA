/* package whatever; // don't place package name! */

import java.util.*;
import java.lang.*;
import java.io.*;

/* Name of the class has to be "Main" only if the class is public. */
class Ideone
{
	public static void main (String[] args) throws java.lang.Exception
	{
		// your code goes here
		string passw;
		string normalchars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ12345467890"
		Scanner s = new Scanner(System.in);
		passw = s.next();
		int lower = 0,upper = 0,digit = 0,special = 0;
		count = passw.length();
		for(int i =0;i<n;i++){
			if(islower(passw[i])
				lower = 1;
			if(isupper(passw[i])
				upper = 1;
			if(isdigit(passw[i])
				digit = 1;
			size_t special1 = input.find_first_not_of(normalChars);
        	if (special != paasw::npos)
            	special = 0;
		}
		
	}
}
